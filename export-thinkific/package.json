{
  "name": "eximious-thinkific-export",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dedicated static (Vite) build path that packages the approved Eximious claims-investigation lesson as a self-contained HTML5 web package for Thinkific. Reuses the exact approved player/engine/lesson source from ../src via the @ alias. Does NOT touch the Next.js production app.",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "@fontsource-variable/fraunces": "^5",
    "@fontsource-variable/inter": "^5"
  },
  "devDependencies": {
    "vite": "^7",
    "@vitejs/plugin-react": "^5",
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4",
    "vite-plugin-singlefile": "^2"
  }
}
